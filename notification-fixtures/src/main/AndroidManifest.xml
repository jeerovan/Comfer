<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application android:label="${fixtureLabel}" android:icon="@android:drawable/ic_dialog_email" android:allowBackup="false">
        <!-- Test-only APK. Explicit shell broadcasts drive real cross-package listener tests. -->
        <receiver android:name="com.jeerovan.fixtures.FixtureReceiver" android:exported="true" />
        <activity android:name="com.jeerovan.fixtures.FixtureActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
